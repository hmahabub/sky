# Copy this file to .env and fill in real values for your environment.
# settings.py reads these via python-decouple; if .env is absent it
# falls back to safe development defaults, so this is optional locally.

SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,asgroupbd.com,skytex.asgroupbd.com,www.skytex.asgroupbd.com
